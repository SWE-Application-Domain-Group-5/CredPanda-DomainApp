@model EliApp.Models.EntryModel
@inject SignInManager<EliApp.Areas.Identity.Data.EliAppUser> SignInManager
@inject UserManager<EliApp.Areas.Identity.Data.EliAppUser> UserManager

@{
    //var user = await UserManager.GetUserAsync(User);
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>EntryModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="accountInvolved" class="control-label">Account Involved</label>
                <input asp-for="accountInvolved" class="form-control" />
                @*<input asp-for="accountInvolved" asp-action="GetUsername" asp-controller="Entry" asp-route-id=@user.GeneratedUserName class="form-control" />*@
                <span asp-validation-for="accountInvolved" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="accountType" class="control-label">Type of Account</label>
                <select asp-for="accountType" class="form-control">
                    <option>Debit</option>
                    <option>Credit</option>
                </select>
                <span asp-validation-for="accountType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="amount" class="control-label">Amount</label>
                <input asp-for="amount" class="form-control" />
                <span asp-validation-for="amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="supportingFile" class="control-label">Supporting Files</label>
                <input asp-for="supportingFile" class="form-control" />
                <span asp-validation-for="supportingFile" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

